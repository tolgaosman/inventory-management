# Leave unset in production — the app falls back to the relative "/api", which
# nginx already proxies to the Laravel backend on the same origin. Only set an
# absolute URL here if the frontend is served from a different origin than the API.
# NEXT_PUBLIC_API_URL=http://178.105.207.98:4004/api
